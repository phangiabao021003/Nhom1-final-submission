<div class="max-w-3xl mx-auto px-4">
  <div class="flex items-center justify-between">
    <h1 class="flex-1 font-bold text-3xl">Client</h1>

    <div class="flex-wrap lg:flex items-center lg:space-x-3">
      <%= link_to clients_path, class: button_class(theme: "transparent") do %>
      All Clients
      <% end %>
      <%= link_to edit_client_path(@client), class: button_class(theme:"primary") do %>
      Edit
      <% end %>
    </div>
  </div>

  <ul class="text-slate-700">
      <li>
        <p class="font-semibold">Store:</p>
        <p><%= @client.store %></p>
      </li>
      <li>
        <p class="font-semibold">Clientphone:</p>
        <p><%= @client.clientphone %></p>
      </li>
      <li>
        <p class="font-semibold">Clientname:</p>
        <p><%= @client.clientname %></p>
      </li>
      <li>
        <p class="font-semibold">Clientemail:</p>
        <p><%= @client.clientemail %></p>
      </li>
      <li>
        <p class="font-semibold">Clientstandard:</p>
        <p><%= @client.clientstandard %></p>
      </li>
  </ul>
</div>
