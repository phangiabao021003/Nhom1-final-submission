<%= form_with(model: product_type) do |form| %>
<% if product_type.errors.any? %>
<div id="error_explanation" class="bg-red-50 p-6 rounded text-red-800">
  <h2 class="text-lg font-semibold mb-3"><%= pluralize(product_type.errors.count, "error") %> prohibited this product_type from being saved:</h2>

  <ul class="list-disc leading-relaxed">
    <% product_type.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

  <div class="mb-6">
        <%= form.label :typeid, class: label_class %>
          <%= form.text_field :typeid, class: input_class %>
      </div>
  <div class="mb-6">
        <%= form.label :typename, class: label_class %>
          <%= form.text_field :typename, class: input_class %>
      </div>

<%= form.submit class: button_class(theme: 'primary') %>

<% if product_type.persisted? %>
<%= link_to "Cancel", product_type, class: "text-neutral-700 underline inline-block ml-3" %>
<% else %>
<%= link_to "Cancel", product_types_path, class:  "text-neutral-700 underline inline-block ml-3" %>
<% end %>
<% end %>
